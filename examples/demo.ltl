// examples/demo.ltl — runnable demo for ltl_blake3

import std::io
use ltl_blake3 as lib

fn main() {
    io::println("ltl_blake3 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}