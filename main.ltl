// ltl-blake3/main.ltl
//
// BLAKE3 parallel hashing over a binary tree.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-blake3 v0.1.0")
    io::println("BLAKE3 parallel hashing over a binary tree.")
    0
}
